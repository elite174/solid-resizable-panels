{
  "name": "solid-panels-root",
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky install",
    "list-staged": "lint-staged"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "prettier": "^2.8.7",
    "typescript": "^5.0.3"
  }
}
